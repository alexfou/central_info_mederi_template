<%= link_to "Central de Información", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Cerrar sesión', destroy_user_session_path, :method=>'delete' %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Ingresar', new_user_session_path %>  
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Editar cuenta', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to 'Administración', users_path %>
      </li>
    <% end %>
  <% else %>
    <li>
    <%= link_to 'Solicitar clave', new_user_registration_path %>
    </li>
  <% end %>
</ul>